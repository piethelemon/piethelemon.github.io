{
    "Twitch_id": {
        "filename": "filename",
        "volume": 1
        }
}

// Così si dovrebbe poter accedere con il nome utente come chiave del dizionario (Object) e poi averne le relative proprietà
// Usare operatore 'in' per controllare se una chiave è esistente
// es.: if("Twitch_id" in Object)...

// Per il random posso estrarre tutte le keys nell'oggetto con la funzione Object.keys(obj) e metterle in un array, per poi
// sceglierne una con la funzione Math.random() (c'è da vedere se è da adattare in base all'output che da).
// Forse questa è anche più leggera da utilizzare per il controllo 'if("Twitch_id" in Object)'